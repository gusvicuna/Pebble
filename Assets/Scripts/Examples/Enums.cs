using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace mygames.supertelegram
{
    // Comentario
    /* Un gran comentario.
     * Que usa hartas lineas.
     * O parrafos si quieren */

    // Enums: Son int, pero con "nombres" para mejor uso.

    //En este caso element tiene ice=0 , fire=1 y water=2
    public enum element
    {
        ice,
        fire,
        water
    }



    public enum difficulty
    {
        easy = 1,
        medium = 5,
        hard = 10
    }
}
